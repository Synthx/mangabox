import 'package:flutter/material.dart';

class SeriesTile extends StatelessWidget {
  const SeriesTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const ListTile(
      onTap: null,
    );
  }
}
